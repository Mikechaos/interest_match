= form_for @cateroty do |f|
  - if @cateroty.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cateroty.errors.count, "error")} prohibited this cateroty from being saved:"
      %ul
        - @cateroty.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
